"""Reusable computational modules for proxy synthesis and meta-analysis.

The package is intentionally dependency-light.  Optional methods (GAM, spatial
statistics, and the R bridge) are imported only when they are requested. The
multi-proxy module provides a conservative common-target integration layer.
"""
